---
dns_domain: "abc"
dns_server_ip: "{{ core_ip }}"
dns_records:
  - { name: "jenkins", ip: "{{ core_ip }}" }
  - { name: "nexus", ip: "{{ core_ip }}" }
  - { name: "sonar", ip: "{{ core_ip }}" }
  - { name: "docker", ip: "{{ core_ip }}" }
